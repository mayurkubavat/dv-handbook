# Chapter 6, worked example: one environment, three tests. Composition --
# a method called through a handle-typed property -- does not compile on
# Icarus 13.0, so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_fifo_env
SOURCES      := ../../ch01-what-is-dv/rtl/fifo.sv fifo_if.sv env.sv \
                tests.sv tb_fifo_env.sv
include ../../common.mk
