# Chapter 6: extending a class. Constructor chaining and hiding run on both
# simulators; the virtual override does not, so this runs on the default.
EXAMPLE_LANG := sv
TOP          := tb_extends
SOURCES      := tb_extends.sv
include ../../common.mk
