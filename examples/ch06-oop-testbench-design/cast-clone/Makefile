# Chapter 6: $cast and clone. `$cast` on class handles is not defined on
# Icarus 13.0, so this runs on the default simulator only. The `new base`
# line prints the trap the chapter describes on Verilator 5.030.
EXAMPLE_LANG := sv
TOP          := tb_cast_clone
SOURCES      := tb_cast_clone.sv
include ../../common.mk
