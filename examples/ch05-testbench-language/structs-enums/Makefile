# Chapter 5: packed structs, unions and enums. A chained method call on an
# enum (`s.next().name()`) is a syntax error on Icarus 13.0 and `$cast` is
# not defined there, so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_structs
SOURCES      := tb_structs.sv
include ../../common.mk
