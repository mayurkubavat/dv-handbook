# Chapter 5: packed structs, unions and enums. `$cast` on an enum is
# unsupported on Icarus 13.0, so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_structs
SOURCES      := tb_structs.sv
include ../../common.mk
