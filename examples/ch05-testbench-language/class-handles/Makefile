# Chapter 5: handles and objects. The class scope operator, `packet::made`,
# is a syntax error on Icarus 13.0 (a static member read through an
# instance is not), so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_handles
SOURCES      := tb_handles.sv
include ../../common.mk
