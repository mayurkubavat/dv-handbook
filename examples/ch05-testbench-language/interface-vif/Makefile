# Chapter 5: an interface with modports, held from a class through a
# virtual interface. Interface ports and virtual interfaces are unsupported
# on Icarus 13.0, so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_vif
SOURCES      := bus_if.sv sink.sv tb_vif.sv
include ../../common.mk
