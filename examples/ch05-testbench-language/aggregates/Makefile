# Chapter 5: arrays, queues and associative arrays. Associative arrays are
# unsupported on Icarus 13.0, so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_aggregates
SOURCES      := tb_aggregates.sv
include ../../common.mk
