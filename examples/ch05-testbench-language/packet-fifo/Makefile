# Chapter 5, worked example: one packet from the port to the scoreboard,
# on the FIFO of Chapter 1. Uses interface ports, a virtual interface and a
# clocking block, so it runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_packet_fifo
SOURCES      := ../../ch01-what-is-dv/rtl/fifo.sv fifo_if.sv packet.sv \
                tb_packet_fifo.sv
include ../../common.mk
