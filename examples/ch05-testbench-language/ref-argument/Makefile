# Chapter 5: a `ref` argument. Icarus 13.0 rejects `ref` ("Reference ports
# not supported yet"), so this runs on the default simulator only.
EXAMPLE_LANG := sv
TOP          := tb_ref
SOURCES      := tb_ref.sv
include ../../common.mk
