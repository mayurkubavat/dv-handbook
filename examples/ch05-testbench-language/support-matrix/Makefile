# Chapter 5: the support matrix, regenerated from the probes on every run.
# Runs every probe under both simulators; about two minutes.
EXAMPLE_LANG := python
TOP          := matrix.py
SOURCES      := matrix.py
include ../../common.mk

clean::
	rm -rf build matrix.md
