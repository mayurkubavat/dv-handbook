---
title: "Chapter 4 gate — Simulation Fundamentals (fifth pass)"
date: 2026-09-10
reviewer: independent gate review (fresh context)
chapter: chapters/ch04-simulation-fundamentals.qmd
evidence: research/ch04-simulation-fundamentals.md
follows: docs/reviews/ch04-gate-d-2026-09-10.md
verdict: blocking findings remain
---

# Chapter 4 gate: Simulation Fundamentals, fifth pass

Scope, in the order the brief sets it: every testable claim re-derived by running
SystemVerilog written for this review rather than by reading the chapter's own
examples; every citation checked against the document fetched from the entry's
own URL, whole author list and whole title; every spliced `run.out` compared
byte-for-byte against a fresh run; every listing checked in the rendered PDF as
well as in the source; the four blockers and fourteen non-blocking findings of
round D quoted from the current text; house rules; originality.

Eleven source documents were fetched and read: `docs/internals.rst` and
`icarus_verilog_quirks.rst` from their upstream `master` branches, both
"gotchas" papers from `sutherland-hdl.com`, the publisher's own paper index at
`sutherland-hdl.com/papers.html`, the 2013 X paper from the DVCon proceedings,
the Cummings and Salz Rev 1.2, the 2000 non-blocking paper, the 2004 two-state
paper and the Verilab clocking paper (the last four from Internet Archive
captures, as their entries' notes say), and the CSL-JSON records behind both
DOIs. Nothing below is taken from the research note.

Working directory for the experiments:
`/private/tmp/claude-501/-Users-mkubavat-Silicon-DV-Book/509cec7a-8e35-4235-bedc-8af42ee71d13/scratchpad/ch04-review-e/`.
Toolchain: Icarus Verilog 13.0 (stable), Verilator 5.030 2024-10-27, conda env
`dvbook`, repository at `fb5e8d5`.

## Verdict

**blocking findings remain.**

**Three blockers.** All three are printed in `_book/Design-Verification.pdf` as
it stands.

Round D's four blockers (D1–D4) are **all fixed**, and D1, D2 and D3 are fixed
correctly. **D4 is fixed by replacing one false cross-module claim with a
different one**: the old text said a finer module's declaration changes a
coarser module's behavior, which is false under Icarus; the new text says it
does not, which is false under Verilator. Both rewrites rested on an experiment
run under one simulator only. That is **E1**.

**E3** is new to this round and was invisible to every previous round because
every previous round read `pdftotext` output rather than the rendered page: the
listing font's programming ligatures fuse `<=` into a single `≤` glyph, so the
book's central worked example prints the non-blocking assignment operator as a
mathematical relational symbol, and the sentence above it — "four `<`
characters" — is refuted by the page beneath it for the second round running,
by a different mechanism than D2's.

### Blocking findings

| # | Where | What |
|---|---|---|
| **E1** | `chapters/ch04-simulation-fundamentals.qmd:174` | "a module's own delays are still rounded to its own declared precision, **so a finer module elsewhere does not change what a coarser one does**". Stated as unhedged fact. **Verilator 5.030 does not do this.** Built and run both ways for this review: the same `1ns/1ns` module's `#1.4` fires at 1.0 ns under Icarus and at 1.4 ns under Verilator, and the difference reorders two prints against a second module. This is round D's D4 with the sign flipped. |
| **E2** | `refs.bib`, `sutherland2007gotchas` (title and `note`) | The entry's title, "More Standard Gotchas: Subtleties in the Verilog and SystemVerilog Standards That Every Engineer Should Know", appears **neither** on the paper's cover **nor** in the publisher's index. The cover reads "Gotcha Again: More Subtleties in the Verilog and SystemVerilog **Standards** That Every Engineer Should Know"; the index reads "More Standard Gotchas: Subtleties in Verilog and SystemVerilog That Every Engineer Should Know" — no "the", no "Standards". The entry is a hybrid of the two, and its `note` asserts it is "cited here by the publisher's index title", which is not what the index says. Fifth consecutive round with an attribution defect; same class as round C's C2, which was blocked. |
| **E3** | `_quarto.yml:45–47`, printed at `_book/Design-Verification.pdf` p. 120 | The monospace face is JetBrains Mono with programming ligatures live, so `<=` renders as a single `≤` glyph and `===`/`==` as `≡`. Printed p. 120 shows `if (!rst_n) count ≤ 8'h00;` in `safe.sv`, directly under the sentence "Change the four assignment operators to non-blocking — **four `<` characters**, and nothing else". There is no `<` character on the page. 233 occurrences book-wide. A book that teaches the difference between `=` and `<=` must print `<=`. |

---

*(sections below appended as the review proceeded)*

## E1 detail — the timescale rewrite is false under the other simulator

`chapters/ch04-simulation-fundamentals.qmd:174`:

> Delays are scaled to the smallest precision used anywhere in the design
> [@sutherland2007gotchas], which sets the resolution of the shared time wheel;
> **a module's own delays are still rounded to its own declared precision, so a
> finer module elsewhere does not change what a coarser one does.**

The first clause is the source's, verbatim in substance (`g2007.txt:1807–1808`,
§8.1: "Within simulation, all delays are scaled to the smallest precision used
by the design"). The bolded clause is the chapter's own inference. It is what
IEEE 1800 requires, and **Verilator 5.030 does not do it.**

Written for this review, not adapted from the chapter's examples. A module that
declares `1ns/1ns` and delays by `#1.4`, alongside a module that declares
`1ns/1ps`. If the coarse module's delay is rounded to its own 1 ns precision it
fires at 1.0 ns, *before* the fine module's 1.2 ns event; if it is not, it fires
at 1.4 ns, *after* it:

```systemverilog
// o_coarse.sv
`timescale 1ns/1ns
module ocoarse;
  initial #1.4 $display("  ocoarse (1ns/1ns): my #1.4 fired");
endmodule

// o_fine.sv
`timescale 1ns/1ps
module ofine;
  initial #1.2 $display("  ofine   (1ns/1ps): my #1.2 fired");
endmodule
```

```
$ iverilog -g2012 -o O.vvp o_coarse.sv o_fine.sv o_top.sv && vvp O.vvp
  ocoarse (1ns/1ns): my #1.4 fired
  ofine   (1ns/1ps): my #1.2 fired

$ verilator --binary --timing -Wno-DECLFILENAME -Wno-TIMESCALEMOD \
      --top-module otop -Mdir vO o_coarse.sv o_fine.sv o_top.sv && ./vO/Votop
  ofine   (1ns/1ps): my #1.2 fired
  ocoarse (1ns/1ns): my #1.4 fired
```

**Opposite orderings, same source.** Isolated further — one `1ns/1ns` module,
nothing else in the design with a delay, and only the top declaring `1ns/1ps`:

```
$ vvp P.vvp                       # iverilog
  pcoarse #1.4 -> $realtime=1.000000 (1ns units)
  pcoarse #0.4 -> $realtime=1.000000 (rounds to 0 if honored)
$ ./vP/Vptop                      # verilator
  pcoarse #1.4 -> $realtime=1.400000 (1ns units)
  pcoarse #0.4 -> $realtime=1.800000 (rounds to 0 if honored)
```

Icarus rounds `#1.4` to 1 ns and `#0.4` to 0 — the module's own declared
precision. Verilator rounds neither; it uses the design-wide precision. The
sentence the chapter prints is a statement about the model, correct as such,
printed as a statement about what happens.

This matters more than an ordinary inaccuracy for three reasons:

1. It is round D's **D4 with the sign reversed.** D4 blocked "Delays are scaled
   to the smallest precision used anywhere in the design, *so one module's
   declaration changes another module's behavior*" on an experiment run under
   Icarus alone. That sentence is false under Icarus and **true under
   Verilator**. Its replacement is true under Icarus and **false under
   Verilator**. Two rounds have now each verified a cross-module timescale claim
   on one of the two simulators the book ships, and each time the tool that was
   not run is the one that contradicts the claim.
2. The very next paragraph (`:176`) tells the reader that the chapter's own
   examples get two different answers from the two tools about what time it is.
   A reader primed by that sentence is exactly the reader who will test this one.
3. The paragraph's own hedge does not cover it. `:174` declines to state a
   rounding rule "because no openly readable source states one" and says
   "measure it on the tool you use" — but that hedge is about *which way* a
   sub-precision delay rounds, not about *whether rounding to the module's own
   precision happens at all*, which is the thing that differs.

*Fix.* Keep the model statement and mark it as one, and let the disagreement do
the work the section wants:

> Delays are scaled to the smallest precision used anywhere in the design
> [@sutherland2007gotchas], which sets the resolution of the shared time wheel.
> In the model, a module's delays are then rounded to that module's own declared
> precision, so a finer module elsewhere should not change what a coarser one
> does — and this is a place worth measuring rather than assuming, because this
> book's two simulators answer it differently.

Everything else in the paragraph is correct. The compile-order half was
independently reproduced for this review: the same file, with no `` `timescale ``
of its own, containing `#100`:

```
$ iverilog -g2012 -o c1.vvp co_ns.sv co_bare.sv co_ps.sv co_top.sv && vvp c1.vvp
  bare_mod: its #100 elapsed, now $realtime=100000      # 100 ns
$ iverilog -g2012 -o c2.vvp co_ps.sv co_bare.sv co_ns.sv co_top.sv && vvp c2.vvp
  bare_mod: its #100 elapsed, now $realtime=100         # 100 ps
```

A factor of a thousand, from the order of the files on the command line. Exactly
what `:174` says, and exactly `g2007.txt:1823–1826`.

---

## E2 detail — a bibliography title that appears in neither place it claims

`refs.bib`, `sutherland2007gotchas`:

```bibtex
  title     = {More Standard Gotchas: Subtleties in the {Verilog} and
               {SystemVerilog} Standards That Every Engineer Should Know},
  note      = {The paper's own cover reads "Gotcha Again"; cited here by the
               publisher's index title. Accessed 2026-09-08}
```

**What the paper says.** Fetched from the entry's own URL,
`sutherland-hdl.com/papers/2007-SNUG-SanJose_gotcha_again_paper.pdf`. Cover
(`g2007.txt:1–3`):

> Gotcha Again
> More Subtleties in the Verilog and SystemVerilog Standards
> That Every Engineer Should Know

`pdfinfo` agrees: `Title: Gotcha Again: More Subtleties in the Verilog and
SystemVerilog Standards That Every Engineer Should Know`.

**What the publisher's index says.** Fetched from `sutherland-hdl.com/papers.html`:

> 2007 SNUG Silicon Valley - Synopsys Users Group Conference: **More Standard
> Gotchas: Subtleties in Verilog and SystemVerilog That Every Engineer Should
> Know** by Stuart Sutherland and Don Mills (Awarded Best Paper)

No "the", no "Standards".

**The entry is a splice of the two.** It takes "More Standard Gotchas:
Subtleties in" from the index and "the Verilog and SystemVerilog **Standards**
That Every Engineer Should Know" from the cover. The resulting string is on
neither document, and the `note` attributes it to the index, which does not
carry it.

This is the fifth consecutive round to find an attribution defect and it is the
same class round C blocked on as **C2** (a wrong title on `cummings2004twostate`).
It also survived round D, which recorded the entry as "**Correct**, and the
`note` about the cover-versus-index title is accurate" — round D compared the
title against the cover and the `note`'s *claim*, and did not fetch the index the
`note` cites.

*Fix.* Use the document's own title, which is also its embedded metadata, and
drop the claim about the index:

```bibtex
  title = {Gotcha Again: More Subtleties in the {Verilog} and
           {SystemVerilog} Standards That Every Engineer Should Know},
  note  = {The publisher's index lists this paper as "More Standard Gotchas";
           cited here by the title on the paper itself. Accessed 2026-09-08}
```

`chapters/ch04-simulation-fundamentals.qmd:254` needs no change: "The two
'gotchas' papers, by Sutherland and Mills and then with Chris Spear on the
second" is correct against both covers, and against `pdfinfo Author` on the 2006
paper ("Stuart Sutherland (Sutherland HDL, Inc.) and Don Mills (Microchip)",
`grep -ci spear g2006.txt` → 0) and the three names on the 2007 cover.

The other nine entries this chapter cites are **correct**, whole author list and
whole title, each checked against the document fetched from the entry's own URL
or DOI. Table in §3 below.

---

## E3 detail — the listing font prints `<=` as `≤`

`_quarto.yml:45–47`:

```yaml
    monofont: "JetBrains Mono"
    monofontoptions:
      - Scale=0.82
```

JetBrains Mono ships programming ligatures in its `calt` feature, which fontspec
enables by default. Nothing turns them off. `pdffonts` confirms the face is
embedded and in use for listings (`QUWHWJ+JetBrainsMono-Regular`).

**What reaches the reader.** `_book/Design-Verification.pdf` p. 120, rendered at
150 dpi and read as an image rather than through `pdftotext` — which is why four
previous rounds did not see this:

> `if (!rst_n) count ≤ 8'h00;`
> `else        count ≤ count + 8'd1;`

directly under, on the same page:

> Change the four assignment operators to non-blocking — **four `<` characters**,
> and nothing else — and the race is gone…

There is no `<` character on the page. The reader is told to look for four of
them and shown four `≤` glyphs. This is round D's **D2** — a count refuted by the
listing beneath it — arriving again by a different route, one round after D2 was
fixed.

It is worse than a count error, because `<=` and `≤` are not interchangeable in
this book's subject matter:

- In an expression, `<=` **is** the less-than-or-equal operator in Verilog. A
  chapter whose entire thesis is the difference between `=` and `<=` prints the
  non-blocking assignment operator as the relational operator it must be
  distinguished from.
- A reader who copies the visible characters does not get compiling SystemVerilog.

**It also corrupts verbatim simulator output**, which the constitution says is
included from `run.out` "so it cannot drift from what the code prints". PDF
p. 112, the regions block:

> `[active]    just assigned b ≤ 1, b is still x`

`examples/ch04-simulation/regions/run.out:2` says:

```
  [active]    just assigned b<=1, b is still x
```

The page is not showing what the simulator printed.

**Scope.** `===` renders as `≡` (PDF p. 116, `≡ four-state, event-driven`, where
`run.out:1` is `=== four-state, event-driven`). `pdftotext` reports 233
occurrences of the `<=` ligature and 9 of the `===` ligature across the whole
book, so this is not a Chapter 4 defect — but Chapter 4 is where a sentence in the
prose counts the characters and is refuted.

*Fix (one line, and it is a whole-book change, so it is an escalation as well as
a finding).* Turn the contextual alternates off in `_quarto.yml:46–47`:

```yaml
    monofontoptions:
      - Scale=0.82
      - RawFeature={-calt,-liga}
```

Re-render and re-read **p. 120 as an image**, not as extracted text: the check
that failed for four rounds is the one that trusts `pdftotext`.

---

## 1. The examples: every `run.out` byte-identical to a fresh run

```
$ git status --porcelain                     # clean apart from this review file
?? docs/reviews/ch04-gate-e-2026-09-10.md
$ bash scripts/check-examples.sh ch04
examples/ch04-simulation/fourstate     lint=pass    run=pass
examples/ch04-simulation/race          lint=pass    run=pass
examples/ch04-simulation/regions       lint=pass    run=pass
examples/ch04-simulation/sampling      lint=pass    run=pass
wrote .../examples/status.json  (pass=4 fail=0 skipped_run=0)
$ git status --porcelain
 M examples/status.json
?? docs/reviews/ch04-gate-e-2026-09-10.md
```

`scripts/check-examples.sh` rewrites each `run.out` in place from the fresh run,
so a `git status` naming only `examples/status.json` afterwards is a
byte-for-byte comparison. Checked independently by MD5 before and after — all
four identical:

```
837d0a23f57101f33f97252a49e27368  race/run.out
e45ffa5d347cdcc9061213cc7ba67c76  regions/run.out
02f9cbc9531f3059c7c3e733a31f9cb3  sampling/run.out
29670c9a1c905c0f36dbc3589862d4a5  fourstate/run.out
```

`examples/status.json` was restored with `git checkout` afterwards; the working
tree is as it was found.

### 1.1 Every prose claim about what an example prints

| Line | Claim | Checked against |
|---|---|---|
| 70 | "the non-blocking target still reads as *not yet updated* in Active, and `$strobe` sees it updated" | `regions/run.out:2,4`. Reproduced independently in §2.2 |
| 70 | "The two Active-region lines may come out either way round… a different simulator may swap them" | **True, and demonstrated.** Committed Icarus capture has `b` first; the same `tb_regions.sv` run under Verilator for this review prints `a` first |
| 70 | "the same testbench on a two-state tool prints a zero in that position" | Run for this review: `[active] just assigned b<=1, b is still 0` |
| 125 | "The watcher reading `q` at the edge sees the value from *before* this edge" | `sampling/run.out:5` `t=15 at the edge : q=10` against `:6` `$strobe : q=11` and `:7` `after it : q=11` |
| 125 | "`d` counts up from ten" | `tb_sampling.sv:24,29` |
| 156 | "the bare `if` leaves its target unknown" | `fourstate/run.out:2` |
| 156 | "The `if ... else` produces 238, which is the value in its else branch" | `fourstate/run.out:3`; `dut_unreset.sv:31` assigns `8'hEE` = 238 |
| 156 | "the whole argument in **four lines**" | **False — E-S1.** The first block is three lines |
| 158 | "both readers give a plausible answer, and would on every run" | `fourstate/run.out:5,6` — both print 2 |
| 158 | "Randomizing says the result depends on the initial state" | `fourstate/run.out:8–10` — 56 / 197 / 14 |
| 176 | the regions output "reports finishing 'at 6 (1s)'" | `regions/run.out:6`, verbatim |
| 176 | "None of them declares a timescale in its source"; "the build passes it a timescale on the command line" | `grep -rn '`timescale' examples/ch04-simulation/*/*.sv` → empty; all seven Verilator invocations carry `--timescale 1ns/1ps` (`fourstate/run.sh:18,24`, `fourstate/Makefile:7`, `regions/Makefile:13`, `sampling/Makefile:9`, `race/run_both.sh:16`, `race/lint_both.sh:13`) |
| 211 | "On the racy design they disagree… On the safe one they report the same thing" | `race/run.out:14,28` |
| 215 | Lint fires on the racing design and the warning is off by default | `race/lint_both.sh:13` passes `-Wall`, `:18` adds `-Wwarn-BLKSEQ` on top of it |

---

## 2. Technical claims, re-derived by running code written for this review

Nothing in this section relies on the chapter's own examples except where the
table above says so.

### 2.1 What control flow does with an unknown (`:145–147`) — all five claims hold

```systemverilog
// e1_x.sv (excerpt) -- xc = 1'bx, xsel = 2'bxx, all targets pre-set to 7
if (xc) bare = 8'd1;                                         // bare if
if (xc) els  = 8'd1; else els = 8'd2;                        // if/else
case (xsel) 2'b00: csn = 8'd1; 2'b01: csn = 8'd2; endcase    // no default
case (xsel) 2'b00: csd = 8'd1; default: csd = 8'd9; endcase  // default
```

```
$ iverilog -g2012 -o e1.vvp e1_x.sv && vvp e1.vvp
bare_if_keeps_old=7 if_else_takes_else=2 case_nodefault=7 case_default=9
x&1=x x&0=0 x|1=1 x|0=x x+1=x x^0=x x==1=x ~x=x
```

| Chapter, `:145–147` | Result |
|---|---|
| "An `if` whose condition is unknown does not take the true branch" | Neither `bare` nor `els` took it |
| "With one, the `else` branch runs, exactly as though the condition had been false" | `if_else_takes_else=2` |
| "Without one, neither branch runs and the target keeps whatever it held before" | `bare_if_keeps_old=7` |
| "A `case` on an unknown selector takes its `default`, or, where it has none, does nothing and leaves its target holding the value it already had" | `case_default=9`, `case_nodefault=7` |
| "Arithmetic and bitwise operators **generally** propagate it" | `x&1=x`, `x+1=x`, `x^0=x`, `x==1=x` propagate; `x&0=0` and `x\|1=1` do not. The hedge earns its place |

### 2.2 Active, Inactive, NBA and Postponed in one timestep (`:52–58`, `:91`, `:107`) — hold

```systemverilog
// e2_nba.sv (excerpt) -- v starts at 5
always @(posedge clk) v <= 8'd99;                              // NBA writer
always @(posedge clk) begin r_active = v; $display(...); end   // Active reader
always @(posedge clk) $strobe("  [strobe]   v=%0d", v);        // Postponed
always @(posedge clk) #0 $display("  [inactive] ... v=%0d", v);// Inactive
```

```
$ vvp e2.vvp                          # event-driven
  [active]   read v=5 immediately at the edge
  [inactive] a #0 statement ran, v=5
  [strobe]   v=99
  [nextstep] v=99

$ ./ve2/Ve2                           # statically scheduled
  [inactive] a #0 statement ran, v=5
  [active]   read v=5 immediately at the edge
  [strobe]   v=99
  [nextstep] v=99
```

`:91` ("the update is in NBA and every reader in Active sees the old value") and
the `$display`-versus-`$strobe` row of the guideline table at `:101` are exact on
both tools. And the run is an unplanned confirmation of `:107`: **the two tools
put the `#0` print on opposite sides of the Active print**, which is precisely
"it does not remove the race; it renames it".

### 2.3 Two blocking writers, one variable (`:63`, `:91`) — holds, and the tools disagree

```systemverilog
// e3_two.sv
always @(posedge clk) v = 8'd1;
always @(posedge clk) v = 8'd2;
```

```
$ vvp e3.vvp        # event-driven
  last writer won: v=1
$ ./ve3/Ve3         # statically scheduled
  last writer won: v=2
```

`:63` ("which runs first is the simulator's choice") and `:91` ("the order is
arbitrary, and the last writer wins") confirmed by construction, on code written
here.

### 2.4 The zero-time loop (`:77`) — holds, with a blocking control

```systemverilog
// e5_loop.sv                         // e6_loopb.sv differs only in the operator
always @(m or n) m <= m + 8'd1;       // e6:  m = m + 8'd1;
initial begin #1 n = 8'd1; #9 $display("  reached time %0t -- NO lock-up", $time); $finish; end
```

```
$ vvp e5.vvp     # non-blocking: killed after 8 s, no output at all, exit=137
$ vvp e6.vvp     # blocking
  reached time 10000 -- NO lock-up
```

Exactly the chapter's claim, and exactly Cummings' argument (`nba.txt:273–279`)
that a blocking assignment completes before the trigger event could be scheduled
and therefore cannot retrigger itself.

### 2.5 What the statically scheduled tool implements (`:182`) — the rewrite is sound

Round D's **D3** ("two regions of nine") is gone. The replacement at `:182` is
checked against `docs/internals.rst` fetched from the entry's own URL:

| Chapter `:182` | `internals.rst` |
|---|---|
| "decides its scheduling at compile time rather than maintaining a queue" | `:225–227` "The static (Verilation time) scheduling of SystemVerilog processes is performed by code in the `V3Sched` namespace" |
| "the Active and non-blocking regions are where the work is placed and iterated to convergence" | `:223–225` "Verilator implements the Active and NBA regions of the SystemVerilog scheduling model"; `:549–556` the `evalAct`/`evalNba` convergence loops |
| "the remaining regions nest around those, each iterating the loops of every region that precedes it in the scheduling order" | `:558–560`, near-verbatim — see **E-S3** |
| "Its option for handling delays and event controls adds the ability to suspend a process" | `:565–570` "Timing support in Verilator utilizes C++ coroutines… These coroutines get suspended at the await" |
| "it does not turn it into an event-driven simulator, and an ordinary clocked block stays statically scheduled" | `:771–772` "All coroutines are committed and resumed in the 'act' region", inside the statically ordered region. Consistent with `race/run.out:10–13`, where the racy design gives the statically ordered `1,2,3,4` with `--timing` on |

`:182`'s Icarus clause is also exact. `quirks.rst:434–438`: "Icarus Verilog
slightly modifies time 0 scheduling by arranging for always statements with
ANYEDGE sensitivity lists to be scheduled before any other threads."

---

## 3. Sourcing: every citation checked against the document at its own URL

Whole author list and whole title compared against the cover page and the PDF
metadata, or against the CSL-JSON record behind the DOI.

| Key | Document as fetched | Entry |
|---|---|---|
| `cummingssalz2007events` | Cover: "SystemVerilog Event Regions, Race Avoidance & Guidelines / Clifford E. Cummings, Sunburst Design, Inc. / Arturo Salz, Synopsys". Footers "SNUG Boston 2006" + "Rev 1.2"; §10.2 "Revision 1.2 (December 2007)". 42 pp. | **Correct** (see E-M6 on `&` vs "and") |
| `cummings2000nba` | Cover: "Nonblocking Assignments in Verilog Synthesis, Coding Styles That Kill! / Clifford E. Cummings / Sunburst Design, Inc." Header "SNUG-2000 San Jose, CA". 23 pp. | **Correct** |
| `cummings2004twostate` | Cover: "SystemVerilog 2-State Simulation / Performance and Verification Advantages / Clifford E. Cummings, Sunburst Design, Inc. / Lionel Bening, Hewlett-Packard". Footers "SNUG Boston 2004". 45 pp. | **Correct** |
| `sutherland2006gotchas` | Cover: "Standard Gotchas / Subtleties in the Verilog and SystemVerilog Standards That Every Engineer Should Know / Stuart Sutherland / Don Mills". `pdfinfo Author:` the same two. Footers "SNUG Boston 2006". `grep -ci spear` → **0**. 63 pp. | **Correct** |
| `sutherland2007gotchas` | Cover: "Gotcha Again / More Subtleties in the Verilog and SystemVerilog Standards That Every Engineer Should Know", Sutherland, Mills (LCDM Engineering), Spear (Synopsys). Footers "SNUG San Jose 2007". 44 pp. | **Title wrong — E2.** Authors, venue and year correct |
| `sutherland2013x` | Cover: "I'm Still In Love With My X! (but, do I want my X to be an optimist, a pessimist, or eliminated?) / Stuart Sutherland". Publisher index lists it under 2013, "DVCon San Jose - Design and Verification Conference"; references accessed 11/2012 | **Correct** |
| `bromley2012clocking` | Cover: "Taming Testbench Timing: Time's Up for Clocking Block Confusions / Jonathan Bromley / Kevin Johnston / Verilab / Austin, Tx". Footers "SNUG 2012", no city | **Correct.** (The PDF's stale embedded metadata reads "DATE 2006 / Frank Poppen"; cover and footers are the right document) |
| `ulrich1969selective` | CSL-JSON at `doi.org/10.1145/362848.362870`: "Exclusive simulation of activity in digital networks", Ernst G. Ulrich, *Communications of the ACM* 12(2):102–110, Feb 1969 | **Correct** (entry gives "E. G. Ulrich"; initials are style) |
| `szygenda1975timebased` | CSL-JSON at `doi.org/10.1109/c-m.1975.218898`: "Digital Logic Simulation in a Time-Based, Table-Driven Environment", S.A. Szygenda and E.W. Thompson, *Computer* 8(3):24–36, Mar 1975 | **Correct.** The ". Part 1" suffix and the unread-text caveat are both disclosed in the entry's own `note` — round D's D-M5 is closed properly |
| `icarus-quirks` | `Documentation/usage/icarus_verilog_quirks.rst`, `master`, lines 434–438 | **Correct**, and `:182` reports it accurately |
| `verilator-internals` | `docs/internals.rst`, `master` | **Correct**, and `:182`'s use of it is now accurate — see E-S3 and E-M3 on form |

### 3.1 Every quotation, verbatim-checked against the source

| Chapter | Quoted words | Found at |
|---|---|---|
| `:43` | "put in place to be the testbench dual of the RTL Active set of event regions" | `cs.txt:1680–1681`, §10.1. **Verbatim.** The per-region claim that follows is carried by `cs.txt:562` ("The Reactive region is the reactive region set dual of the corresponding Active region in the same time slot") and `:589` |
| `:132` | "surprisingly error-prone" | `bromley.txt:22` (abstract) and `:120` (§1). **Verbatim** |
| `:149` | "not an abstraction of actual silicon values" | `x2013.txt:41` "The fourth value, X, is not an abstraction of actual silicon values." **Verbatim** |

Three quotations in the whole chapter, each a fragment of one sentence, each in
quotation marks with its key on the same line. Within the constitution's limit.

### 3.2 Every sourced claim, checked against the sentence it carries

| Line | Claim | Source text |
|---|---|---|
| 28 | Time advances only when no events remain | `cs.txt` §2. Key is on the callout |
| 35 | Selective simulation described in the 1960s | Ulrich's title and abstract carry it on their face |
| 35 | "table-driven, time-based logic simulators were being built in the 1970s" | Narrowed to exactly what the title supports. **Round D's D-S3 is fixed**, and `:254` now says plainly that the paper is paywalled and unread |
| 45 | Reactive holds `program` code and assertion action blocks | `cs.txt:560–570`, §2.2.8: "Execute all program blocking assignments. Execute the pass/fail code from concurrent assertions" |
| 58 | Postponed: `$strobe`/`$monitor`, read-only | `cs.txt:656–661`: "execute the `$strobe` and `$monitor` commands… There is no feedback path from the Postponed region back into the RTL or Reactive-loop regions" |
| 60 | Inactive was needed before NBA existed; good code stopped populating it | `cs.txt:499–500` footnote 3, "circa 1989"; `:482` "the Inactive region is unused in the rest of the event scheduling examples in this paper" |
| 77 | NBA in a combinational block locks simulation up | `g2007.txt:335` "Gotcha: My RTL simulation locks up and time stops advancing." Reproduced in §2.4 |
| 89 | At time zero all procedural blocks activate in any order, no precedence | `g2006.txt:1832` "Initial procedural blocks can activate in any order relative to always procedural blocks"; `:1842` "Verilog initial blocks have no precedence over always blocks" |
| 93 | The guidelines are derived region by region | `cs.txt` attaches "Guideline #3" to §2.2.3 (Active, `:467`), "#8" to §2.2.4 (Inactive, `:476`), "#6" to §2.2.4 (`:479`), "#1" to §2.2.5 (NBA, `:491`); and `nba.txt:268–270` states the method outright: "The event queues will also be referenced to justify the eight coding guidelines given in section 5.0" |
| 104 | Guideline #5 is a corollary, not a region consequence | `nba.txt:843–846`: "As a matter of forming good coding habits, the author encourages adherence to the following: Guideline #5…", introduced from a synthesis error, not from a region. **The carve-out is correct** |
| 107 | `#0` does not remove the race | `nba.txt` #8 and `g2006.txt:2642–2643` "The Verilog #0 construct is an easily abused construct, and does not truly ensure the delayed statement will execute after all other statements in a given time step". Two independent sources, as the chapter says. Independently reproduced in §2.2 |
| 109 | The one honest exception, removed by a newer construct | `g2006.txt:2646–2652`, the `->>` nonblocking event trigger. Exact |
| 129 | The skews sit where a flip-flop's setup window and clock-to-output delay are | `bromley.txt:192–193` "sample signals one setup time before the clock event; update signals one clock-to-output delay after the clock event" |
| 132 | The two mistakes: clockvar-versus-signal, wrong assignment operator | `bromley.txt:302` ("the most important single cause of mistakes in the use of clocking blocks") and `:417` ("Never try to write an output clockvar using simple assignment =") |
| 137 | Sample one step before the edge and the testbench agrees with its assertions | `bromley.txt:460` "input#1step yields exactly the same value as … assertions clocked by the same clock event" |
| 147 | X-optimism: the unknown is silently converted into a decision | `x2013.txt:41–46`. Reproduced in §2.1 |
| 164 | 2001, 2.4M gate equivalents, ~6%, 15% heard and not seen, the authors' own caution | `twostate.txt:1267–1273` ("In March 2001… on a ~2.4M gate-equivalent RTL chip model… About a 6% performance improvement"), `:1389–1390` ("we have heard of some design teams that claim upwards of 15%… we have not seen this level"), `:1182–1183` ("It would be a mistake to quote the performance figures from this paper even 6 months from now"). **All four present; round D's D-S2 is fixed and is now the strongest-sourced paragraph in the chapter** |
| 174 | Delays scale to the smallest precision used anywhere | `g2007.txt:1807–1808`. The clause after it is **E1**; the clause after that is **E-S2** |
| 174 | The compile-order trap | `g2007.txt:1823–1826`. Reproduced in §E1 |
| 182 | Icarus's time-zero departure | `quirks.rst:434–438`. Exact |
| 217 | Three ways two tools can differ on one file | `g2006.txt:121–126`: implementation freedom; scheduling that "allows tools to optimize order of concurrent events differently"; and "Some tools are not 100% standards compliant. This is not a gotcha in the standards…". The three-way split is the source's own, and `:217–219` remains the most valuable passage in the chapter |
| 254 | Rev 1.2 carries SNUG Boston 2006 on every page though dated December 2007 | Every footer pair in `cs.txt` reads "SNUG Boston 2006" / "Rev 1.2"; `:75` heads §10.2 "Revision 1.2 (December 2007)". **Round D's D-S1 is fixed** |

---
