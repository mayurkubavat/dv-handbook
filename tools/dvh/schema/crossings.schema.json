{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mayurkubavat.github.io/dv-handbook/schema/crossings-1.json",
  "title": "dvh crossings",
  "description": "Crossings between domains. No verdict here is a sign-off.",
  "type": "object",
  "required": [
    "crossings",
    "unrecognized"
  ],
  "additionalProperties": false,
  "$defs": {
    "crossing": {
      "type": "object",
      "required": [
        "to",
        "to_domain",
        "from",
        "from_domain",
        "width",
        "direct",
        "second_stage",
        "shape_recognized",
        "note",
        "src",
        "same_clock_source"
      ],
      "additionalProperties": false,
      "properties": {
        "from": {
          "description": "The sending register.",
          "type": "string"
        },
        "from_domain": {
          "description": "Its clock domain.",
          "type": "string"
        },
        "to": {
          "description": "The receiving register.",
          "type": "string"
        },
        "to_domain": {
          "description": "Its clock domain.",
          "type": "string"
        },
        "width": {
          "description": "Width of the receiving register, in bits.",
          "type": "integer",
          "minimum": 1
        },
        "direct": {
          "description": "Direct to D on every clock; false behind an enable.",
          "type": "boolean"
        },
        "second_stage": {
          "description": "Same-domain registers fed directly by this one.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "src": {
          "description": "Source location, as file:line.col-line.col.",
          "$comment": "Constrained so a blank or a non-location cannot pass.",
          "type": "string",
          "pattern": "^[^:]+:\\d+"
        },
        "shape_recognized": {
          "description": "True if this matches a known shape.",
          "type": "boolean",
          "$comment": "Two-flop, and not one of several across a boundary."
        },
        "note": {
          "description": "Empty when known; else what was seen.",
          "type": "string"
        },
        "same_clock_source": {
          "description": "Both ends: one declared net, with one driver.",
          "$comment": "The net, not the name: a vector port's bits share one.",
          "type": "boolean"
        }
      }
    }
  },
  "properties": {
    "crossings": {
      "description": "Every crossing found, safe or not.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/crossing"
      }
    },
    "unrecognized": {
      "description": "All but same_clock_source; absence is not correctness.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/crossing"
      }
    }
  },
  "$comment": "Reports a recognized shape, never whether a crossing is safe."
}
